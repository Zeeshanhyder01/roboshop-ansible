- name: Download {{ COMPONENT }} data
  ansible.builtin.get_url:
    url: https://github.com/roboshop-devops-project/{{ COMPONENT }}/archive/main.zip
    dest: /tmp/{{ COMPONENT }}.zip


- name: Deleting old content
  ansible.builtin.file:
    path: "{{ APP_LOCATION }}"
    state: absent

